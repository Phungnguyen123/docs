# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Runtime output & logs (keep the folders, ignore contents)
output/*.xlsx
output/_progress*.jsonl
logs/*.log

# Keep the sample input, ignore other local inputs
input/*.xlsx
!input/companies.xlsx

# Hong Kong dataset files are large & downloaded locally; keep the folder readme
input/hk/*.csv
input/hk/*.xlsx
input/hk/*.xls
!input/hk/README.txt

# Secrets — never commit
.env

# Real watchlist may contain internal asset lists; keep only the example
input/watchlist.json
